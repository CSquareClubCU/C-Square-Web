# Task Tracker

## Phase 1 — Foundation (Backend)

- `[ ]` Initialize Python virtual environment and `requirements.txt`
- `[ ]` Scaffold Django project in `/backend`
- `[ ]` Create Django apps (`core`, `users`, `events`, `registrations`, `attendance`, `team`)
- `[ ]` Configure `settings.py` (PostgreSQL, DRF, django-sesame, timezone, apps)
- `[ ]` Implement `core.models.BaseModel` and custom API exception handler
- `[ ]` Implement custom `users.models.User`
- `[ ]` Implement Magic Link Auth logic (`users/services.py`)
- `[ ]` Create `/api/auth/` endpoints (request link, verify, logout, me)
- `[ ]` Configure Azure Email Service integration stub
- `[ ]` Write automated tests for user auth flows
